# 《记账花哪去了》小程序隐私保护指引填报模板

> **用途**：登录微信公众平台 → 设置 → 用户隐私保护指引 → 配置《小程序隐私保护指引》时，按本模板填写。
> **小程序信息**：名称「记账花哪去了」 / AppID `wxdffed438b6bd8235` / 主体：个人

---

## 一、需要声明收集的个人信息

| 信息类型 | 对应接口 / 场景 | 用途 | 必要性 |
|---|---|---|---|
| 微信 OpenID | `wx.login` → `login` 云函数 | 标识用户身份，隔离各人记账数据 | 必需 |
| 记账业务数据（金额 / 分类 / 备注 / 发生日期 / 收/支类型） | 用户输入 + 云数据库 `records` 集合 | 提供记账、统计报表、账单管理核心功能 | 必需 |
| 用户自定义分类 | 用户在「分类管理」中添加的分类名称、图标、识别关键词 | 用于个性化分类展示与语音/导入自动匹配 | 可选 |
| 麦克风（录音功能） | 小程序录音 `wx.getRecorderManager` + 云函数 `asr` 调腾讯云「一句话识别」 | 语音记账：说一句话自动解析预填，仅本次识别不留存 | 可选 |

> ⚠️ **关键**：`wx.getRecorderManager` 属于隐私接口，微信要求隐私保护指引中**勾选「麦克风 / 录音功能」这个权限 scope**。只写使用说明、不勾选 scope，会在调用录音时直接报 `api scope is not declared in the privacy agreement`（start/stop 都会被平台拦截，代码层无法绕过）。

> 本小程序**不**收集手机号、真实姓名、地理位置、通讯录、人脸等敏感信息；**不**使用微信支付；**不**接入广告 SDK；**不**向第三方提供数据。

---

## 二、可直接复制的「收集使用说明」正文

```
我们高度重视您的个人信息与数据安全。在使用《记账花哪去了》小程序时，我们仅收集实现核心功能所必需的信息：

1. 微信 OpenID：用于标识您的身份，确保您的记账数据仅您本人可见，不与他人混淆。
2. 记账数据：包括您主动录入的收支金额、分类、备注、发生日期与收支类型，存储于微信云开发数据库，用于为您提供记账、统计报表与账单管理功能。
3. 自定义分类（可选）：您在「分类管理」中添加的分类名称、图标与识别关键词，存储于微信云开发数据库，用于个性化展示与语音/导入自动匹配。
4. 语音输入（可选）：当您使用语音记账时，语音内容经小程序录音后由云端语音识别服务实时转换为文字，仅用于本次记账识别，不作留存与训练。

我们不会将上述信息用于广告推送或提供给任何第三方。您可以随时在小程序内删除您的记账记录，也可联系开发者申请注销与数据清除。
```

---

## 三、语音记账（已启用，云端 ASR 方案）

语音记账**不再依赖任何微信插件**，改用小程序自带录音 + 云函数调用腾讯云「一句话识别」：

- `pages/record/record.js` 用 `wx.getRecorderManager()` 录音（16k PCM），停止后上传云存储
- 云函数 `cloudfunctions/asr/index.js` 下载录音 → TC3 签名调腾讯云 ASR → 返回文本
- 文本交 `utils/voice-parser.js` 解析后预填表单（只预填，不自动存）

**前置配置**（开发者在云开发控制台操作，用户无感）：
1. 注册腾讯云账号并开通「语音识别 ASR」服务
2. 获取 API 密钥 SecretId / SecretKey
3. 在云函数 `asr` 的「配置 → 环境变量」中设置 `TENCENT_SECRET_ID`、`TENCENT_SECRET_KEY`
4. 上传并部署 `asr` 云函数

隐私指引已按上方第一节 / 第二节追加「语音输入」一条（描述改为云端识别，不再提插件）。

⚠️ **连带动作**：语音方案变更 = 隐私指引内容变更，需在后台「用户隐私保护指引」重新提交审核（约 1 工作日），通过后才能发布带语音的版本。

---

## 四、后台操作步骤

1. 登录 [mp.weixin.qq.com](https://mp.weixin.qq.com) → 你的小程序 → **设置** → **用户隐私保护指引**
2. 点击「配置指引 / 更新」，**务必在「需收集的个人信息」列表中勾选「麦克风 / 录音功能」这项 scope**（这是平台拦截录音接口的直接原因；仅写文字说明不够）
3. 同时勾选上述其他收集项（OpenID、业务数据、自定义分类），并在对应「麦克风」项的用途说明里粘贴第二节第 4 条
4. 在自定义说明栏整体粘贴**第二节**文本
5. 提交，通常 1 个工作日内审核通过（审核通过前弹窗已能正常显示，但版本不可发布；**声明麦克风前语音记账会报错，属预期**）

---

## 五、代码侧已完成的合规动作（供核对）

- `app.json` 已开启 `"__usePrivacyCheck__": true`
- 已新增 `components/privacy-modal` 隐私授权弹窗组件
- `app.js` 已注册 `wx.onNeedPrivacyAuthorize` 全局监听，用户同意时调用 `resolve({ event: 'agree' })`
- 4 个 tab 页（记一笔 / 统计 / 账单 / 我的）均已引入隐私组件，首次进入自动弹出，同意后不再重复
