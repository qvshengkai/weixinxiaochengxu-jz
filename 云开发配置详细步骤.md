# 云开发配置详细步骤（建集合 + 部署云函数）

> 目标：让「记账花哪去了」小程序真正能存数据、出报表。
> 前置：云开发已开通、app.js 已填环境 ID `cloud1-d2g32eu370f4219df`、工程已导入并能编译。
> 本操作全部在**微信开发者工具**本机完成，无法远程代点。

---

## 第一部分：新建 `records` 集合（数据库表）

`records` 就是存每一笔账的地方。

**第 1 步** — 打开云开发控制台
- 在开发者工具**顶部工具栏**找到「**云开发**」按钮（一朵云的图标，在「预览 / 真机调试」附近）。
- 点击它，会弹出一个**云开发控制台**窗口（第一次可能要等几秒加载）。

**第 2 步** — 进入数据库
- 云开发控制台**左侧菜单**点「**数据库**」。
- 右侧会出现「集合列表」区域（一开始是空的）。

**第 3 步** — 新建集合
- 集合列表**右上角**点「**+ 新建集合**」。
- 在弹出的输入框里填集合名称：`records`（小写，英文，和代码里完全一致）。
- 点「确定」。

**第 4 步** — 设置权限（重要）
- 在集合列表里点 `records` 这一行，进入集合详情。
- 找到「**权限设置**」标签（或页面里的权限下拉）。
- 选择「**仅创建者可读写**」。
- 点「保存 / 确定」。

> 为什么是"仅创建者可读写"：每个用户只能看到**自己**的账。我们的报表、账单都按 `_openid` 隔离，选这个权限最安全也最合规。

**第 5 步** — 确认
- 回到集合列表，应能看到 `records`，且记录数为 0。✅ 第一部分完成。

---

## 第二部分之前：先给 `cloudfunctions` 根目录绑定云环境（必做，否则报「请选择云环境」）

> 这就是你刚才报错「请在编辑器云函数根目录（cloudfunctionRoot）选择一个云环境」的根因。**只需做一次**，绑完后三个函数部署都不用再选。

**第 0 步** — 右键 cloudfunctions 根目录选环境
- 在左侧文件树，找到 **`cloudfunctions`** 这个**根文件夹**（注意是它本身，不是里面的 login/aggregate/importBill）。
- **右键**它，菜单**顶部**会显示你的云环境列表，找到 `cloud1-d2g32eu370f4219df` 并**点选**它。
  - 如果右键后**没出现环境列表**、而是直接弹错误：先点顶部「**云开发**」打开控制台，确认环境存在且状态正常，关掉控制台后再右键 `cloudfunctions` 根目录，列表就会出现。
  - 选中后该根目录会显示已绑定的环境名（如 `cloud1-d2g32eu370f4219df`）。

**第 0.5 步** — 确认
- 绑定成功后，再右键 `cloudfunctions/login` 就不会再报「请选择云环境」了。

---

## 第二部分：部署 `login` 云函数

`login` 用来在云端拿到用户的 openid（静默登录，无需用户点授权）。

**第 1 步** — 回到代码编辑区
- 关掉（或最小化）云开发控制台，让左侧**小程序代码文件树**可见。
- 文件树里能看到 `cloudfunctions/` 文件夹，下面有 `login`、`aggregate`、`importBill` 三个子文件夹。

**第 2 步** — 右键 login 文件夹
- 在 `cloudfunctions/login` 这个**文件夹**上点**鼠标右键**（注意是文件夹，不是里面的 index.js）。

**第 3 步** — 选择部署方式
- 在右键菜单里选「**上传并部署：云端安装依赖**」。
  - ⚠️ 一定要选带「**云端安装依赖**」的那一项，不要选「本地安装依赖」（你本机多半没配 node 环境，本地装会失败）。

**第 4 步** — 等待部署
- 底部会出现「**上传部署**」日志面板，显示 `npm install` 和上传进度。
- 过程约 **10–30 秒**，期间不要切走。

**第 5 步** — 确认成功
- 日志最后出现「**上传成功**」或「部署完成」字样，即完成。

**第 6 步** — 在控制台验证
- 重新打开「云开发」控制台 → 左侧「**云函数**」。
- 列表里应能看到 `login`，状态不是「未部署」。

**第 7 步（可选）— 测试 login**
- 在云函数列表点 `login` → 「**测试**」→ 运行。
- 返回结果里应包含你的 `openid`（一串字符），说明函数通了。

---

## 第三部分：部署 `aggregate` 和 `importBill`

这两个是报表统计和账单导入用的，**步骤和第二部分完全一样**，只是换个文件夹：

- 右键 `cloudfunctions/aggregate` → 「上传并部署：云端安装依赖」→ 等成功
- 右键 `cloudfunctions/importBill` → 「上传并部署：云端安装依赖」→ 等成功

> 建议**一个一个来**，别同时右键三个，避免日志混在一起看不清是否都成功。
> 三个都部署完后，云函数列表里应能看到 `login`、`aggregate`、`importBill` 三个，状态均为已部署。✅ 第三部分完成。

---

## 第四部分：验证整体是否就绪

1. **云函数列表**：`login` / `aggregate` / `importBill` 三个都在，无「未部署」。
2. **数据库**：`records` 集合存在，权限=仅创建者可读写。
3. **重新编译**：在开发者工具点「**编译**」（或「预览」重新生成二维码）。
4. 之前控制台可能飘的 `login failed` 红字，部署完重新编译后应消失。

---

## 第五部分：常见失败 & 解决办法

| 现象 | 原因 | 解决 |
|------|------|------|
| 右键没有「上传并部署」选项 | 文件夹没被识别为云函数 | 确认 `project.config.json` 里有 `"cloudfunctionRoot": "cloudfunctions/"`（已配好）；重新导入工程 |
| 部署卡在「安装依赖」很久 | 网络慢或 npm 源问题 | 等久一点（1–2 分钟）；不行就取消重来一次 |
| 报 `wx-server-sdk not found` | 云端没装上依赖 | 确认选的是「**云端安装依赖**」；检查 `package.json` 里有 `wx-server-sdk`（已确认有） |
| 部署成功但调用报「找不到函数」 | 小程序端缓存 | 点「编译」重新构建；或清缓存（工具栏→清缓存→全部清除）再编译 |
| 保存记账报 `collection records not exists` | 集合没建或名字拼错 | 回到第一部分，确认集合名是 `records`（小写英文），不是 `Records`/`record` |
| 保存成功但报表/账单看不到 | 权限设错 | 集合权限必须=「仅创建者可读写」 |
| 上传失败 / 网络错误 | 网络波动 | 重试；关闭代理/VPN 再试 |
| 调用云函数报 `errCode: -504002`、`0 code exit unexpected` | **函数依赖 `wx-server-sdk` 没装进云端**（最常见：部署时误选了「本地安装依赖」、或云端 `npm install` 实际没跑/失败） | 见下方「专项：解决 -504002」 |
| 调用报 `Error: cloud.callFunction:fail ... functions execute fail` | 一般是上面的依赖崩，或函数抛异常 | 先按 -504002 处理；若依赖已确认装好仍报错，看云函数日志里的真实堆栈 |

### 专项：解决 -504002（0 code exit unexpected）

这是**函数启动即崩溃**，不是业务逻辑错。云函数运行时要 `require('wx-server-sdk')`，如果云端没这个包，进程一加载就退出，前端就收到 `-504002`。**你之前在开发者工具里重部署没解决，说明工具的「云端安装依赖」这次没真正把包装进去**（这是工具已知会卡的点）。改用**网页控制台**最稳，在那儿能直接看到依赖在不在。

**✅ 修法一（首选 · 网页控制台，能直接看到依赖状态）：**
1. 浏览器打开 **微信云开发控制台**（开发者工具顶部「云开发」→ 右上角有「在网页控制台打开」图标，或直开 `cloud.weixin.qq.com` 用同一账号登录选环境 `cloud1-d2g32eu370f4219df`）。
2. 左侧「**云函数**」→ 点 `aggregate`，进入「**函数代码**」标签页。
3. 看页面左侧（或右侧）的「**依赖 / 目录**」面板：
   - **没有 `wx-server-sdk`** → 点「**云端安装依赖**」（或「保存并安装依赖」「安装依赖」按钮，名字随版本略有差异）→ 等它跑完，底部出现 `install completed` 即成功。
   - **已有 `wx-server-sdk`** 还报 -504002 → 点「**测试**」按钮跑一次，看返回的**真实错误堆栈**（那种情况就不是依赖问题了，把堆栈发我）。
4. 对 `login`、`importBill` 重复同样检查（三个函数都要有 `wx-server-sdk`，否则它们调用时也会 -504002）。

**✅ 修法二（核选项 · 工具顽固不装依赖时）：**
如果网页控制台里 `aggregate` 的依赖面板也乱/装不上，直接重建：
1. 网页控制台「云函数」→ 勾选 `aggregate` → 「**删除**」。
2. 回到**开发者工具**，右键 `cloudfunctions/aggregate` → 「**上传并部署：云端安装依赖**」，这次盯紧底部日志直到 `npm install` 完成。
3. 回到网页控制台确认 `aggregate` 依赖里出现 `wx-server-sdk`。

**验证是否修好：** 重新编译小程序 → 进「报表」页 → 应能调通；或在控制台「测试」`aggregate` 传 `{}` 会返回 `code:1 missing range`（说明函数已能正常启动，只是没传参数），也算装好了。

---

## 第六部分：部署完后，测试记账闭环

1. 在开发者工具点「**预览**」→ 手机扫码打开小程序。
2. **记一笔**：切到「记账」页 → 选分类（如餐饮）→ 用数字键盘输金额（如 35）→ 点「保存」→ 应提示「已记录」。
3. **看报表**：切到「报表」页 → 选「本周」→ 应能看到金额和分类占比。
4. **看账单**：切到「账单」页 → 应能看到刚记的那笔 35 元。
5. （可选）**导账单**：账单页点「导入账单」→ 选微信导出的 CSV → 提示导入成功 → 账单页多出记录。

任何一步报错，把**控制台红字**或**手机上的提示文字**发给我，我帮你定位。
