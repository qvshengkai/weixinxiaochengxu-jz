# 微信小程序账号开通 + 工程运行 · 从零一步一步指南

面向：**还没有任何微信小程序账号**的情况。按本文顺序操作，约 30 分钟能注册好并跑通我们的记账小程序（开发预览阶段无需备案，详见第 7 步）。

---

## 0. 先做一个关键决策：用「个人」还是「企业」主体？

小程序账号注册时就要选主体类型，**注册后很难改**，所以先定。

| 对比项 | 个人主体（推荐先用） | 企业主体 |
| --- | --- | --- |
| 需要什么 | 身份证 + 个人微信 | 营业执照 + 企业对公账户 |
| 认证费 | 免费 | 300 元/年 |
| 能不能发布上线 | ✅ 能 | ✅ 能 |
| 云开发（我们工程用的） | ✅ 能用 | ✅ 能用 |
| 微信支付 | ❌ 不能（个人无商户号） | ✅ 能 |
| 部分类目/营销能力 | 受限 | 全开 |
| 适合阶段 | MVP 跑通、验证手感 | 商业化、要支付时 |

> **结论**：我们的记账 MVP 一期**不用微信支付**（数据走「手动/语音/导入账单」），所以**先用「个人主体」注册最划算**——免费、几分钟搞定。等后面要做会员付费、家庭账本商业化，再升级企业主体也不迟。

---

## 1. 注册小程序账号（约 10 分钟）

1. 电脑浏览器打开 **https://mp.weixin.qq.com**（微信公众平台）。
2. 右上角点 **「立即注册」**。
3. **账号类型一定选「小程序」**（不是订阅号/服务号/企业微信，别选错）。
4. **填邮箱**：用一个**没被任何微信公众号/个人微信绑定过**的邮箱（QQ 邮箱、163 都行）。
5. 去邮箱点激活链接，回到页面设置**密码**。
6. **主体信息登记**：
   - 选「个人」；
   - 填你的姓名、身份证号；
   - 用**你本人实名过的微信**扫码确认（扫码的微信就是以后的「管理员」）。
7. 注册完成，自动进入小程序后台。

> 💡 记好这个邮箱+密码，以后登录后台就用它。

---

## 2. 拿到 AppID（工程要填的第一个空）

1. 在后台左侧点 **「开发」→「开发管理」→「开发设置」**。
2. 页面顶部有一串 `wx` 开头的字符串，就是 **AppID**（例如 `wx1a2b3c4d5e6f7g8h`）。
3. 复制它，待会儿填进工程。

---

## 3. 下载安装「微信开发者工具」

1. 打开 **https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html**
2. 下载 **「稳定版 Stable Build」**（按你系统选 Windows 64 位）。
3. 双击安装，一路下一步。
4. 打开工具，用**管理员微信**扫码登录。

---

## 4. 导入我们的工程（填 AppID）

1. 工具首页点 **「导入项目」**。
2. **目录**选：`D:\workbuddy\记账小程序\`（就是我给你搭好的工程文件夹）。
3. **AppID** 那一栏：选「填写 AppID」→ 粘贴第 2 步复制的 `wx...`。
   - 别用「测试号」！测试号**不能开通云开发**，工程跑不起来。
4. 后端服务：**勾选「微信云开发」**（重要）。
5. 点「导入」。

导入后工程会报错（因为还没填云环境 ID），正常，继续下一步。

---

## 5. 开通云开发（填第二个空：云环境 ID）

1. 工具顶部点 **「云开发」** 按钮（第一次会弹开通引导）。
2. 按提示开通，给环境起个名（如 `prod-1`），**付费方式选「按量计费」**（新用户有免费额度，MVP 测试几乎不花钱）。
3. 开通后进入「云开发控制台」，左上角能看到**环境 ID**（格式如 `prod-1-7gxxxxxxx`）。复制它。
4. 打开工程里的 `app.js`，把第 6 行：
   ```js
   env: 'your-cloud-env-id'
   ```
   改成你的环境 ID：
   ```js
   env: 'prod-1-7gxxxxxxx'
   ```
5. 打开工程里的 `project.config.json`，把 `appid` 字段也确认成你的 AppID（导入时填过一般已自动写入）。

> ⚠️ 改完保存（Ctrl+S），工具会自动重新编译。

---

## 6. 建数据库集合 + 部署云函数

1. **建集合**：云开发控制台 → **「数据库」** → 新建集合，名字填 `records` → 创建后点进 `records` → **「权限设置」** → 选 **「仅创建者可读写」**。
2. **部署云函数**（3 个，逐个来）：
   - 在工具左侧文件树，右键 `cloudfunctions/login` → **「上传并部署：云端安装依赖」**；
   - 同样右键 `cloudfunctions/aggregate` → 上传并部署；
   - 同样右键 `cloudfunctions/importBill` → 上传并部署。
   - 每个约 10–30 秒，提示「上传成功」即可。

---

## 7. 添加语音插件（语音记账用，可选）

1. 浏览器回到小程序后台 **mp.weixin.qq.com**。
2. 左侧 **「设置」→「第三方设置」→「插件管理」→「添加插件」**。
3. 搜索 **「微信同声传译」** → 申请添加（免费，秒过）。
4. 不加也能跑工程，只是记账页的🎤按钮会提示「未启用插件」；手动/报表/导入都不受影响。

---

## 8. 真机预览（扫码即可，无需备案）

1. 工具顶部点 **「预览」** → 用手机微信扫码。
2. 手机上就能真机试：记一笔 → 切到报表看环比同比 → 导入微信账单。

> ✅ **到此你已经能完整使用了。** 开发预览阶段**不需要备案**，可以一直调试。

---

## 9. 想正式发布上线？还要做 3 件事（非开发必需）

只有当你要「提交审核、别人在微信里搜到并使用」时才需要：

1. **小程序备案**（2023年9月起强制）：后台「设置 → 小程序备案」提交，需填主体信息+域名（云开发自带域名，**不用自己买服务器**），管局审核约 1–20 天。⚠️ 这是上线最大时间变量，建议尽早发起。
2. **隐私保护指引**：后台「设置 → 用户隐私保护指引」→ 勾选并说明「收集你手动录入的交易信息用于记账统计」→ 提交发布。**切忌写「自动同步微信账单」**，否则审核必拒。
3. **提交审核**：工具里点「上传」→ 后台「版本管理」提交审核 → 等 1–2 天。类目选 **「工具 → 记账」**。

---

## 常见坑（提前避）

- ❌ **用测试号导入**：测试号开不了云开发，工程必报错。一定要用自己的 AppID + 开通云开发。
- ❌ **AppID 和云环境 ID 搞混**：AppID 填在 `project.config.json`，云环境 ID 填在 `app.js` 的 `globalData.env`，两处不同。
- ❌ **集合权限设成「所有用户可读」**：会导致别人能看到你的交易。务必「仅创建者可读写」。
- ❌ **宣称自动同步微信账单**：违规，审核必拒。我们只做手动/语音/导入。
- ❌ **没装云函数依赖**：右键部署时务必选「云端安装依赖」，否则函数运行报 `require` 错。
- 💡 **改了 env 不生效**：改完 `app.js` 后按 Ctrl+S 保存，工具会自动重编译。

---

## 一句总结操作顺序

> 注册小程序（个人）→ 拿 AppID → 装开发者工具 → 导入工程填 AppID → 开通云开发拿环境 ID 填 app.js → 建 records 集合 → 部署 3 个云函数 → 预览扫码。

完成第 8 步就能用了。要正式给别人用，再补第 9 步的备案+审核。
